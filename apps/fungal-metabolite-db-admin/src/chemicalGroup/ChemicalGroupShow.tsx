import * as React from "react";

import {
  Show,
  SimpleShowLayout,
  ShowProps,
  DateField,
  TextField,
  ReferenceManyField,
  Datagrid,
  ReferenceField,
} from "react-admin";

import { CHEMICALGROUP_TITLE_FIELD } from "./ChemicalGroupTitle";

export const ChemicalGroupShow = (props: ShowProps): React.ReactElement => {
  return (
    <Show {...props}>
      <SimpleShowLayout>
        <DateField source="createdAt" label="Created At" />
        <TextField label="ID" source="id" />
        <TextField label="name" source="name" />
        <DateField source="updatedAt" label="Updated At" />
        <ReferenceManyField
          reference="Metabolite"
          target="chemicalGroupId"
          label="Metabolites"
        >
          <Datagrid rowClick="show" bulkActionButtons={false}>
            <ReferenceField
              label="chemicalGroup"
              source="chemicalgroup.id"
              reference="ChemicalGroup"
            >
              <TextField source={CHEMICALGROUP_TITLE_FIELD} />
            </ReferenceField>
            <DateField source="createdAt" label="Created At" />
            <TextField label="ID" source="id" />
            <TextField label="name" source="name" />
            <DateField source="updatedAt" label="Updated At" />
          </Datagrid>
        </ReferenceManyField>
      </SimpleShowLayout>
    </Show>
  );
};
